{
  "name": "play-together-game-server",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "node --check src/index.js"
  },
  "devDependencies": {
    "wrangler": "4.120.0"
  }
}
